_repos:
    - "{{ main_repo }}"
    - "{{ oms_prod_repo }}"
    - "{{ oms_dev_repo }}"
zabbx_host_groups: 
    - "{{ group_names }}"
host_template:
    - Template ICMP Ping
    - Template OS Linux
    - Template App HTTP Service
  
